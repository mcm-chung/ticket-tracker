import React, { useState } from "react";
import styles from "./Card.module.scss";
import data from "../data/teams";

const Card = (props) => {
  const cardMap = team.map()
  const cardMap = [
    { id } { name }, { role }
  ];

  return (
    <div className="cards">
      {cardMap.map() => (

      )}
    </div>
  )
  

};
export default Card;
